1.1.1	2009-06-06      (the "horse=yes" release)

	* Don't overwrite all other non-iamhere query parameters when generating
          permalink/permahash. (bugfix)

	* Better error messaging when reverse geocoding fails. (update)

	* Made all functions camel-case; added wrappers for backward
        * compatibility. (update)

        * Added a 'refresh_title' parameter to update document title when the
          map changes. (new)

	* Added a 'coordinates_decimal_points' parameter to trim lat/lon display
          to a fixed number of decimal points. (new)
         
	* (less bad) documentation, including HTML output. (new)

1.1	2009-06-05	(the "Stamen" release)
        
	* Added proper zIndex to map controls so they work when they
	  intersect a shapefile. (bugfix)
          
	* Remove support for 'find_my_location' on startup. (update; backwards
          incompatible)

	* Rename 'cloudmade_style' to more generic 'map_style'. (update;
          backwards incompatiable)

        * Display pretty-printed latitude and longitude. (new)

        * Generate auto-updating permalinks and read a limited set of query
          parameters via the URL's hash component. (new)

	* Added a proper Changes file. (new)

1.02	(uh...)

	* Um, stuff?

1.01	(uh...)

	* Um, stuff?

1.0	(uh...)

	* Initial public release
